# CSV transcription visualiser

Small python script to generate a HTML file using Jinja and rcssmin.
The script parses a csv file of a transcription generated by Openai whisper and creates a visualisation.
